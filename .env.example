# 复制为 .env.development 供本地 npm run dev 使用（.env* 已在 .gitignore 中忽略）
#
# Cloudflare Pages Production 请在 Dashboard → Settings → Environment variables 配置同名变量

# development | production
NEXT_PUBLIC_APP_ENV=development

# 本地 Playwright 试抓（需 npm run dev:api）
NEXT_PUBLIC_SCRAPE_ENABLED=true
